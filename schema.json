{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "description": { "type": ["string", "null"] },
      "lineage": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "stage":        { "type": "string",
                              "enum": ["source_onprem", "staging", "dwh", "cloud"] },
            "table":        { "type": ["string", "null"] },
            "column":       { "type": ["string", "null"] },
            "doc_link":     { "type": ["string", "null"] },
            "offline_path": { "type": ["string", "null"] }
          },
          "required": ["stage", "table", "column"],
          "additionalProperties": false
        }
      }
    },
    "required": ["lineage"],
    "additionalProperties": false
  }
}